# Documentation not tracked in version control
running.md

# Python bytecode cache
__pycache__/
archive/
dump/
*.pyc
*.pyo
TODO*

# Per-step event logs are regenerated by every training run and are
# too large to track (up to hundreds of MB). The compact, git-trackable
# artifacts are condensed_events.log, visitation_counts.npy,
# config.json and metrics.csv.
results/raw_data/**/events.log
